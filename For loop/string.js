function str(input) {
    let niz=input[0];
    for(let i=0; i<niz.length;i++){
      //  console.log(i); // отпечатва индексите на низа 
       // let tex=niz[i]; // използване на променлива 
       console.log(niz[i]); 
    }
    //console.table(input);  // Извежда низа в таблица с индексите и стойности
}
//str (["softuni"]);
//str (["ice cream"]);
//str([`H`,`E`,`L`,`L`,`O`]);